# Environment — never commit secrets
.env
.env.local
.env*.local

# Foundry build artifacts (regenerated by forge build)
out/
cache/

# Foundry broadcast cache (contains sensitive tx data)
cache/

# Frontend
frontend/.next/
frontend/node_modules/
frontend/.env.local

# macOS
.DS_Store
